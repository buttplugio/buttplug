name: Buttplug
author: Kyle Machulis <kyle@machul.is>
about: Sex Toy Control Server
args:
    - address:
        short: a
        long: address
        value_name: ADDR
        help: network address/port to listen on (defaults to localhost:12345)
        takes_value: true
        required: true
    - debug:
        short: d
        long: debug
        value_name: LEVEL
        help: Debug output level (DEBUG, INFO, WARN, ERROR)
        takes_value: true
